<div class="recency">
  This data is for the <strong><%= Contribution.latest_cycle %></strong> election cycle.
</div>

<div class="scrollable">
  <table class="grid">
    <% (['top_industries'] + industries).in_groups_of(4).each do |row| %>
      <tr>
        <% row.each do |industry| %>
          <% if industry %>
            <td>
              <%= field_checkbox :contribution, industry, :bold => (industry == 'top_industries') %>
            </td>
          <% end %>
        <% end %>
      </tr>
    <% end %>
  </table>
</div>